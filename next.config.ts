import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
  // output: 'export',
  // distDir: 'out',
  // trailingSlash: true,
  // devIndicators: false,
  // assetPrefix: './',
  // images: {
  //   unoptimized: true,
  // },
}

export default nextConfig
